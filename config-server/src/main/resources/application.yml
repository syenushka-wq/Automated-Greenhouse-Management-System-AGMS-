server:
  port: 8888

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/syenushka-wq/Automated-Greenhouse-Management-System-AGMS-.git
          default-label: main
          clone-on-start: true
      allowOverride: true

# Optional: log level for debug
logging:
  level:
    org.springframework.cloud: DEBUG


