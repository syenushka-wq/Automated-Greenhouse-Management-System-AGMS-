spring:
  application:
    name: api-gateway
  cloud:
    config:
      uri: http://localhost:8888
      username: config-user
      password: config-password

---
spring:
  config:
    activate:
      on-profile: default
  cloud:
    config:
      enabled: true